import 'package:flutter/material.dart';

class AppColors {
  // Primary (Growth & Prosperity)
  static const Color primary = Color(0xFF1B5E20); // Forest Green

  // Secondary (Trust & Security)
  static const Color secondary = Color(0xFF102A43); // Midnight Blue

  // Accent (Innovation/Highlights)
  static const Color accent = Color(0xFFFFC107); // Golden Yellow

  // Background (Clean/Sleek)
  static const Color background = Color(0xFFF5F7FA); // Off-white

  // Light UI
  static const Color lightGrey = Color(0xFFE2E8F0);

  // Text Colors
  static const Color textPrimary =
      Color(0xFF102A43); // Use Midnight Blue for text
  static const Color textSecondary = Color(0xFF486581);

  // Semantic Colors
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFA000);
}
